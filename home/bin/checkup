#!/bin/bash

# Check for internet connectivity.
if ! ping -c 1 -W 1 8.8.8.8 &>/dev/null; then
  exit 0
fi

ups=$(checkupdates | wc -l)
ups_aur=$(yay -Qua | wc -l)
total=$((ups + ups_aur))

[ "$total" -eq 0 ] && exit 0

notify-send -t 10000 "󰏔 Updates" "$total update(s)"
