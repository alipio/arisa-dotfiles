#!/bin/sh

main() {
  option=$(printf "sleep\nreboot\nshutdown" | dmenu)

  case "$option" in
    sleep)
        slock systemctl suspend
        ;;
    reboot)
        systemctl reboot
        ;;
    shutdown)
        sudo shutdown now
        ;;
    *)
        exit 1
        ;;
  esac
}

main
